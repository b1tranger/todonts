# Walkthrough - To-Don't Updates

We have completed the requested changes:
1. **Task Auto-Reordering**: Streak-based tasks (daily constraints and persistent habits) now auto-reorder in ascending order of streaks (lowest streak/highest priority at the top) within their contiguous checklist chunks whenever a task is checked.
2. **Background Notifications**: The Service Worker now runs periodic background checks (every 6 hours for random active task alerts and every 24 hours for backlog alerts) utilizing shared Cache Storage.
3. **Editor Auto-Scroll**: When the Live Editor is toggled open, it automatically scrolls past the YAML Front Matter block so you can edit your tasks immediately.
4. **Documentation**: Updates have been fully integrated into `documentation.md` and `guide.md`.

## 🛠️ Details of the Editor Auto-Scroll Implementation
- Updated the `#toggle-editor-btn` click handler inside [app.js](Documents/GitHub/[personal]/todonts/app.js) to measure the editor textarea style (`line-height`) and automatically set `editor.scrollTop` to scroll past `AppState.yamlLinesCount + 1` lines whenever the editor pane is opened.

## 🧪 Verification
- Verified that toggling the editor pane open automatically scrolls the textarea past the YAML front matter (`--- ... ---` header) to show the start of the tasks at the top.
