# Walkthrough - Close Window Confirmation, Sidebar Exit Button, and Live Editor YAML Auto-Scroll

All tasks for close window confirmations, mobile responsive sidebar layout enhancements, and precise Live Editor YAML auto-scrolling have been completed and verified.

## 🛠️ Changes Implemented

### HTML structure
- **[index.html](Documents/GitHub/%5Bpersonal%5D/todonts/index.html)**:
  - Wrapped `install-btn` and added the new `close-btn` inside a flex container `<div class="sidebar-footer-actions">` to align them on desktop and push them neatly to the bottom on mobile.

### Styling & Layout
- **[style.css](Documents/GitHub/%5Bpersonal%5D/todonts/style.css)**:
  - Structured `.sidebar-footer-actions` with horizontal flex layout for desktop and vertical flex column for mobile.
  - Set `margin-top: auto` on `.sidebar-footer-actions` on mobile to position it at the bottom of the drawer.
  - Increased bottom padding of `.action-buttons` on mobile to `6rem` to prevent the app status footer from covering the bottom buttons.
  - Enabled `overflow-y: auto` on `.action-buttons` on mobile to ensure all menu options are scrollable and reachable.

### Javascript App Logic
- **[app.js](Documents/GitHub/%5Bpersonal%5D/todonts/app.js)**:
  - **YAML Comment Injection**: Enhanced `loadDataFromMarkdown()` to automatically inject `<!-- TASKS START HERE -->` at the start of the body text if it's missing, guaranteeing it is always present right after the closing `---` of the YAML Front Matter.
  - **Interactive Parser Filter**: Updated `compileTextLines()` to filter out and ignore `<!-- TASKS START HERE -->` comment from rendering in the Interactive View.
  - **Close App / Back Interception**: Implemented `initCloseConfirmation()` (wired in `boot()`) to check for PWA mode:
    - If in PWA standalone mode, the Close App button is shown. Clicking it prompts `"Cofirm closing window?"` and calls `window.close()`.
    - Pushes a sentinel state `{ noBackExitsApp: true }` to browser history. When `popstate` triggers, prompts `"Cofirm closing window?"` to close the window or re-pushes state if cancelled.
    - Adds a `beforeunload` listener to prompt standard browser confirmations during window closing/reload.
  - **Live Editor Auto-Scroll**: Refactored the `#toggle-editor-btn` click handler to find `<!-- TASKS START HERE -->` in the textarea, compute the exact number of lines before it, and set the textarea's `scrollTop` based on line height. It applies this scroll inside `requestAnimationFrame` on a 50ms timeout to ensure the layout has stabilized.

## 🧪 Verification Results

- Tested that `app.js` compiles without any syntax errors.
- Verified that `<!-- TASKS START HERE -->` is invisible in the Interactive Tasks view.
- Verified that toggling the Live Editor scrolls precisely to the first line of the tasks, completely bypassing the YAML Front Matter on both desktop and mobile resolutions.
- Simulated PWA mode and verified that the Close App button is visible in the mobile drawer and triggers the `"Cofirm closing window?"` prompt.
